logging.level.org.springframework: DEBUG
spring:
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/SumitBhawsar/global-api-config'
          username: ${git.username}
          password: ${zgit.password}
          timeout: 60